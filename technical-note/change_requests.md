# Change Requests

- This initial draft is intentionally prose-first and does not yet include figures.
- A later pass can add a canonical figure set once the visual evidence surface is frozen.
- The repo's curated benchmark summary reports `2.95x` `2048`-bit candidate-loop speedup, while the detailed benchmark report computes `2.91x` from committed per-candidate mean timings. A later pass should reconcile that presentation if one benchmark surface is chosen as canonical.
